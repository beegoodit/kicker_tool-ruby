en:
  activerecord:
    models:
    attributes:
      shared: &shared
        id: ID
        created_at: Created at
        updated_at: Updated at
  classes:
    kicker_tool/engine: Kicker Tool
  routes:
    kicker-tool-engine: kicker-tool
